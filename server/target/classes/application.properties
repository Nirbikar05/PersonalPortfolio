# Server configuration
server.port=${PORT:8081}

# For detailed error messages (disable in production)
server.error.include-message=always
server.error.include-binding-errors=always

# CORS configuration
spring.mvc.cors.allowed-origins=${ALLOWED_ORIGINS:*}
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true 

# Email configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:therealvickyy1489@gmail.com}
spring.mail.password=${MAIL_PASSWORD:sxgbifzjwrxmfjjx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
email.recipient=${EMAIL_RECIPIENT:nirbikar05@gmail.com}

# Security configuration
server.servlet.session.cookie.secure=${COOKIE_SECURE:false}
server.servlet.session.cookie.http-only=true 